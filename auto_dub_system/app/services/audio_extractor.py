"""
Extract audio from video (ffmpeg).
"""
import subprocess

def extract_audio(video_path: str, output_path: str):
    # subprocess.run(["ffmpeg", "-i", video_path, "-vn", "-acodec", "libmp3lame", output_path])
    pass
